package com.saikiran.pulse.perception.audio

/**
 * Environmental sound category types monitored by Pulse.
 */
enum class SoundType {
    SPEECH,
    FOOTSTEPS,
    VEHICLE,
    VEHICLE_HORN,
    DOOR,
    DOORBELL,
    ALARM,
    SIREN,
    UNKNOWN,
}
